# 작업 기록 — 2026-05-17 18:23

## 작업 요약
- Supabase PostgreSQL에서 SERIAL 시퀀스 미동작 문제 근본 원인 파악 및 완전 수정
  - `init_db()`에 PostgreSQL DO 블록 추가 — 앱 시작 시 모든 med 테이블 시퀀스 자동 생성/복구
  - 기존 id=NULL 행 자동 복구 (`UPDATE ... WHERE id IS NULL`)
- 모든 med 테이블 INSERT에 명시적 id 적용 (`_med_next_id()` 헬퍼 함수)
  - `med_clients`, `med_products`, `med_stock_in`, `med_stock_out`, `med_payments`, `med_contracts`
- 전역 `_safe_id()` 함수 추가 — None/NaN/0 값 안전 변환
- 모든 int(id) 변환 안전 처리 및 DELETE/UPDATE NULL id 방어
- 납품 등록 루프에서 NaN 숫자 입력 크래시 방어 (`_n()` / `_s()` 헬퍼)
- 거래처 원장 `cid2=None` 시 명확한 경고 메시지 추가
- 계약서 양식 영구 보관 구조 개선
  - 날짜 prefix 제거 → 고정 경로 `_contract_template_.docx`로 Storage 저장
  - `med_settings` DB 의존성 제거 — Storage 직접 확인
  - 세션 캐시로 중복 다운로드 방지
  - 양식 확인(다운로드) / 교체 / 삭제 기능 추가
- Python 3.14 `date` 모듈 shadowing 오류 수정 (`drive_upload` 내 로컬 import)

## 생성 / 수정된 파일
- `2.TheSC-MSO 사이트/app.py` — 전체 med 섹션 수정 (SERIAL 수정, 안전 변환, 계약서 양식)

## 스크린샷
![작업 화면](2026-05-17_18-23.png)

## 다음 할 일
- 납품 등록 / 매입 등록 실제 테스트 (Streamlit Cloud)
- 수금 현황 연동 확인
- 계약서 양식 업로드 후 생성 테스트
